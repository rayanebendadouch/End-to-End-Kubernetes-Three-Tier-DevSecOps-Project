apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 3500 #
      targetPort: 3500 # deployment port
      nodePort: 30600
  type: NodePort